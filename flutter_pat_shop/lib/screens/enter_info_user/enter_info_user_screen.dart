import 'package:flutter/material.dart';

class EnterInfoUserScreen extends StatelessWidget {
  const EnterInfoUserScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
